{
  "name": "fcr-x402-contracts",
  "version": "0.1.0",
  "description": "Solidity contracts for FCR-x402 Bond + Deferred Hybrid Model + ERC-8004",

  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network calibration",
    "deploy:erc8004": "hardhat run scripts/deploy-erc8004.ts --network calibration",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "hardhat": "^2.28.0",
    "typescript": "^5.5.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.14.0",
    "dotenv": "^16.4.0"
  }
}
